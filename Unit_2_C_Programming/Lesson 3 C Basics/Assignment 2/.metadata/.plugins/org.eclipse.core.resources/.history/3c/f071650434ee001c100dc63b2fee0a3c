/*
 * main.c
 *
 *  Created on: Jun 16, 2022
 *      Author: ibrah
 */

# include "stdio.h"

int main ()
{

	    int num;
		int sum = 0;

		printf("Enter an integer: ");
		fflush(stdout); fflush(stdin);
		scanf("%d",&num);
		int i;
		for (i =1 ; i<=num ; i++){
			sum+=i;
		}

		printf("Sum = %d",sum);

}
